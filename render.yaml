services:
  - type: web
    name: kuaid-center
    env:
      node:
        runtime: node
        buildCommand: npm install
        startCommand: npm start
        envVars:
          - key: NODE_ENV
            value: production
          - key: PORT
            value: 5000